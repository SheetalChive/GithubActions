name: Docker Build & Push

on:
    workflow_dispatch
#   push:
#     branches:
#       - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Code checkout
        uses: actions/checkout@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ vars.DOKERHUB_USER }}
          password: ${{ vars.DOKERHUB_TOKEN }}

      - name: Build and Push to Docker Hub
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true 
          tags: |
            ${{ vars.DOCKERHUB_NAME }}/github-actions-app:latest 
            ${{ vars.DOCKERHUB_NAME }}/github-actions-app:${{ github.sha }}